import { StyleSheet } from "react-native";

const fontStyles = StyleSheet.create({
    title: {
      fontSize: 70,
      fontWeight: "bold",
      color: "#fff"
    },
    subTitle: {
        fontSize: 60,
        // fontWeight: "bold",
        color: "#eee"  
    }
});

export default fontStyles;
