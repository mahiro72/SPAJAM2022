import { StyleSheet } from "react-native";

const fontStyles = StyleSheet.create({
    title: {
      fontSize: 70,
      fontWeight: "bold",
      color: "#fff"
    },
    subTitle: {
        fontSize: 60,
        // fontWeight: "bold",
        color: "#eee"  
    },
    timerText:{
        fontSize: 40,
        fontWeight: "bold",
        color: "#555"  
    }
});

export default fontStyles;
